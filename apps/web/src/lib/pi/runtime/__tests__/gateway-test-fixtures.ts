/** Synthetic Neon AI Gateway host for tests (not a real branch endpoint). */
export const TEST_NEON_AI_GATEWAY_HOST = "gateway.test.neon.tech"

export const TEST_NEON_AI_GATEWAY_BASE_URL = `https://${TEST_NEON_AI_GATEWAY_HOST}`

export const TEST_NEON_AI_GATEWAY_BASE_URL_V1 = `${TEST_NEON_AI_GATEWAY_BASE_URL}/v1`
