Vite 8 + TanStack Start (SSR) + React 19 + TypeScript 6; Tailwind CSS v4 via `@tailwindcss/vite`; Vitest + happy-dom for unit tests; Playwright for E2E; shadcn/ui with `@workspace/hax-design` component library; `@vercel/nft` for function tracing; `@asteasolutions/zod-to-openapi` for OpenAPI spec generation; `tsx` for running TS scripts directly; `better-sqlite3` and `@neondatabase/serverless` for local/remote DB access.
